# This file can be empty. Its presence makes Python treat the
# `causal_agent` directory as a package, allowing us to import
# its modules (like `graph`, `state`, etc.) from other parts of our application. 